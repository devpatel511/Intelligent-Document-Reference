# Dataset 16 – OS-like Filesystem

Simulates a Linux filesystem layout with deeply nested config,
logging (date-partitioned), scripts, systemd units, and docs.
Tests the indexer's handling of system-style directory hierarchies.
