# Microservices Project

8 microservices with nested source/test/config directories plus
shared infrastructure (Docker Compose + Kubernetes manifests).
Tests directory traversal across many sibling and nested folders.
